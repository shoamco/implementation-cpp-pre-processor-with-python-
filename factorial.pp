void func_of_iostream();
void func_of_iostream();

int factorial(int n)
{
    cout << "Ola" << endl;
    cout <<   MAX(a, b) (a) > (b) ? (a) : (b)
(a, b) (a) > (b) ? (a) : (b)
(3,5) << endl;

	if (n > 1)
	{
		return(n * factorial(n - 1));
	}
   	else if (n == 1)
   	{	
    	return 1;
   	}
   	else
   	{
		return 0;
	} 
}
