
int factorial(int n)
{
    cout << "Ola" << endl;
    cout <<  MAX(3, 5) (3) > (b) ? (3) : (b)
) << endl;

	if (n > 1)
	{
		return(n * factorial(n - 1));
	}
   	else if (n == 1)
   	{	
    	return 1;
   	}
   	else
   	{
		return 0;
	} 
}
